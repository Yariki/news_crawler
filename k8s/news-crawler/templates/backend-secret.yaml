apiVersion: v1
kind: Secret
metadata:
  name: {{ include "news-crawler.backend.secretName" . }}
  labels:
    {{- include "news-crawler.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  DATABASE_URL: {{ include "news-crawler.database.url" . | quote }}